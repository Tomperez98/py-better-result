[project]
name = "example-retry"
version = "0.1.0"
description = "Retry example for py-better-result"
requires-python = ">=3.12"
dependencies = ["py-better-result"]

[tool.uv]
package = false

[tool.uv.sources]
py-better-result = { workspace = true }
