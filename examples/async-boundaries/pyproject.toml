[project]
name = "example-async-boundaries"
version = "0.1.0"
description = "Async boundary helpers example for py-better-result"
requires-python = ">=3.13"
dependencies = ["py-better-result"]

[tool.uv]
package = false

[tool.uv.sources]
py-better-result = { workspace = true }
