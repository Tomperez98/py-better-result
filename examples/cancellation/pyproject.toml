[project]
name = "example-cancellation"
version = "0.1.0"
description = "Cancellation example for py-better-result"
requires-python = ">=3.13"
dependencies = ["py-better-result"]

[tool.uv]
package = false

[tool.uv.sources]
py-better-result = { workspace = true }
