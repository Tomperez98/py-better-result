[project]
name = "example-basic"
version = "0.1.0"
description = "Basic py-better-result workflow example"
requires-python = ">=3.12"
dependencies = ["py-better-result"]

[tool.uv]
package = false

[tool.uv.sources]
py-better-result = { workspace = true }
