[project]
name = "example-sync-retry"
version = "0.1.0"
description = "Synchronous retry example for py-better-result"
requires-python = ">=3.13"
dependencies = ["py-better-result"]

[tool.uv]
package = false

[tool.uv.sources]
py-better-result = { workspace = true }
